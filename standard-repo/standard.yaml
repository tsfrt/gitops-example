apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageRepository
metadata:
  name: tanzu-standard
  namespace: package-repo
spec:
  fetch:
    imgpkgBundle:
      image: #@ data.values.image_registry+"/packages/standard:v2023.7.13_update.2"
      secretRef:
        name: #@ data.values.credentials_secret